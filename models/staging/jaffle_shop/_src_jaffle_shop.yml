sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
        # no freshness config at all — skip the override entirely
      - name: orders
